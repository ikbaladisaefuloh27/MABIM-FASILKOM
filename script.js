function Login() {
    var done = 0;
    var username = documen.login.username.value;
    var password = documen.login.username.value;
}
if(username == "himasi" && password == "himasi") {
    if(confirm("apakah ingin lo0gin") == true) {
        window.location.href ="https://github.com/ikbaladisaefuloh27";
    }
    else{
        alert ("membatalkan login!");
    } 
} 
    else {
    if(done==0){
        alert("username/password salah:")
    }
}